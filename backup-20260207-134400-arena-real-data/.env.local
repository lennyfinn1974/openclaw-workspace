# Trading Platform Configuration

# API URL for the backend server
NEXT_PUBLIC_API_URL=http://localhost:8101

# WebSocket URL for real-time data
NEXT_PUBLIC_WS_URL=http://localhost:8101

# Server Port
PORT=8101

# ===================== LIVE MARKET DATA CONFIGURATION =====================

# Enable/disable live market data (default: true)
# Set to 'false' to use simulation only
# ENABLE_LIVE_DATA=true

# ===================== STOCK DATA (Yahoo Finance) =====================
# Yahoo Finance is used by default for stock data (FREE, no API key needed)
# Provides: Real-time quotes, historical candles
# Rate limit: ~2000 requests/hour (automatically managed)
# No configuration needed - works out of the box!

# ===================== CRYPTO DATA (Binance) =====================
# Binance is used for cryptocurrency data (FREE, no API key needed)
# Provides: Real-time quotes, candles, order book
# Supported: BTC, ETH, SOL, BNB, XRP, ADA, DOGE, AVAX, DOT, LINK, etc.
# Rate limit: 1200 requests/minute (automatically managed)
# No configuration needed - works out of the box!

# ===================== ALPACA (Optional - Enhanced Stock Data) =====================
# Alpaca provides enhanced stock data and real order book
# Sign up at: https://alpaca.markets
# Uncomment and add your keys for real-time L2 data:

# ALPACA_API_KEY=your_api_key_here
# ALPACA_API_SECRET=your_api_secret_here
# ALPACA_DATA_URL=https://data.alpaca.markets/v2

# Paper trading URL (for testing order execution)
# ALPACA_BASE_URL=https://paper-api.alpaca.markets

# ===================== EODHD (Professional Real-Time Data) =====================
# EODHD provides professional-grade real-time data with <50ms latency
# Coverage: 1,100+ FX pairs, 1,000+ crypto, all major stocks & commodities
# Perfect for the 21-bot Wargames Arena genetic evolution system

EODHD_API_KEY=6987021366ef79.20431409
EODHD_BASE_URL=https://eodhd.com/api
EODHD_WS_URL=wss://ws.eodhistoricaldata.com/ws

# Set EODHD as primary data source
PRIMARY_DATA_SOURCE=eodhd

# ===================== ALTERNATIVE DATA SOURCES (Future) =====================

# Polygon.io - Premium real-time data  
# POLYGON_API_KEY=your_api_key_here

# Alpha Vantage - Free tier available (limited requests)
# ALPHA_VANTAGE_API_KEY=your_api_key_here

# ===================== DATA SOURCE PRIORITY =====================
# The system automatically selects the best available data source:
#
# FX (Alpha Group - 7 pairs):
#   1. EODHD WebSocket /ws/forex (real-time streaming)
#   2. Forex Simulator (fallback)
#
# STOCKS (Beta Group - 7 symbols):
#   1. EODHD WebSocket /ws/us-quote (real-time streaming)
#   2. EODHD REST (candles, on-demand quotes)
#   3. Yahoo Finance / Alpaca (fallback)
#
# COMMODITIES (Gamma Group):
#   Gold/Silver: EODHD /ws/forex as XAUUSD/XAGUSD (real-time)
#   Oil/Gas/Copper: EODHD REST polling every 15s
#   LTHM: EODHD /ws/us-quote (real-time)
#   Simulator (fallback for all)
#
# CRYPTO:
#   1. Binance (primary - better coverage)
#   2. EODHD /ws/crypto (fallback)
#
# Check /api/market/sources endpoint for current data source status
